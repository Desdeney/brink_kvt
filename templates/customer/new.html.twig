{% extends 'base.html.twig' %}

{% block title %}Neuer Kunde - Brink VA{% endblock %}

{% block body %}
    <div class="page-header">
        <h1 class="page-title">
            <i class="fas fa-user-plus text-primary"></i>Kunden anlegen
        </h1>
        <div class="page-actions">
            <a href="{{ path('app_customer_index') }}" class="btn btn-outline-secondary btn-standard shadow-sm">
                <i class="fas fa-arrow-left me-2"></i>Zurück zur Liste
            </a>
        </div>
    </div>

    <div class="row justify-content-center">
        <div class="col-lg-8">
            <div class="card data-card shadow mb-4">
                <div class="card-header py-3" style="background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); border: none;">
                    <h6 class="m-0 font-weight-bold text-white"><i class="fas fa-user-plus me-2"></i>Kundeninformationen</h6>
                </div>
                <div class="card-body p-4">
                    {{ include('customer/_form.html.twig') }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
