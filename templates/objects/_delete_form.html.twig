<form method="post" action="{{ path('app_objects_delete', {'id': object.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ object.id) }}">
    <button class="btn btn-danger w-100 shadow-sm" style="border-radius: 10px;">
        <i class="fas fa-trash-alt me-2"></i> Objekt löschen
    </button>
</form>
