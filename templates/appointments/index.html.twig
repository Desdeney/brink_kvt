{% extends 'base.html.twig' %}

{% block title %}Termine{% endblock %}

{% block header %}Termine{% endblock %}

{% block body %}
    <div class="table-container mt-5">
    <table class="table">
        <thead>
            <tr>
                <th>Anlass</th>
                <th>Vorname</th>
                <th>Nachname</th>
                <th>Location</th>
                <th>Datum</th>
                <th>Start</th>
                <th>Ende</th>
                <th>Gäste Anmerkungen</th>
                <th>DJ Anmerkungen</th>
                <th>Aktionen</th>
            </tr>
        </thead>
        <tbody>
        {% for appointment in appointments %}
            <tr>
                <td class="text-nowrap">{{ appointment.occasion }}</td>
                <td class="text-nowrap">{{ appointment.customer.prename }}</td>
                <td class="text-nowrap">{{ appointment.customer.lastname }}</td>
                <td class="text-nowrap">{{ appointment.location.locationName }}</td>
                <td class="text-nowrap">{{ appointment.date ? appointment.date|date('d.m.Y') : '' }}</td>
                <td class="text-nowrap">{{ appointment.startTime ? appointment.startTime|date('H:i') : '' }} Uhr</td>
                <td class="text-nowrap">{{ appointment.endTime ? appointment.endTime|date('H:i') : '' }} Uhr</td>
                <td class="text-nowrap">{{ appointment.attendeesNotes }}</td>
                <td class="text-nowrap">{{ appointment.djNotes }}</td>
                <td class="text-nowrap">
                    <a class="special-link" href="{{ path('app_appointments_show', {'id': appointment.id}) }}">Anzeigen</a>
                    <a class="special-link" href="{{ path('app_appointments_edit', {'id': appointment.id}) }}">Bearbeiten</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="23">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    </div>
    <div class="mt-4"></div>
    <a class="special-link" href="{{ path('app_appointments_new') }}">Neuen Termin anlegen</a>
{% endblock %}
